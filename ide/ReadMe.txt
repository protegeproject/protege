This is a directory for files that support the setup of various
integrated development environments.  This readme may also include
directions for setting up those development environments.

Eclipse:
   At the top level directory, do a "mvn clean install".  Unzip
   ide/eclipse-by-tim.zip at the top level directory and the projects
   should be ready to import into eclipse.

